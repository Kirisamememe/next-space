# Database Configuration
POSTGRES_USER=root
POSTGRES_PASSWORD=mysecretpassword
POSTGRES_DB=local

# Database URL for Next.js
# ローカル開発時は localhost:5433 を使用（DockerのPostgreSQLポート）
# 以下の形式で実際の値を設定してください: postgresql://USER:PASSWORD@localhost:5433/DBNAME
DATABASE_URL=postgresql://root:mysecretpassword@localhost:5433/local
